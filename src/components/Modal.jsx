const Modal = ({ setShowModal }) => {
	return (
		<div className='w-[100px] h-[100px] bg-white none'>
			dcdcdsd
			<button type='button' onClick={() => setShowModal(false)}>
				Close
			</button>
		</div>
	)
}

export default Modal
